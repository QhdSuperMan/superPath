interface Todo {
    title: string;
    // description: string;
    // completed: boolean;
    // createdAt: number;
}
// type TodoPreview = Omit<Todo, "description" | "completed">;
interface Todo {
    // title: string;
    createdAt: number;
}

var a:Todo = {
    title: 's',
    
}